# Python
__pycache__/
*.pyc

# local virtualenv (build-only, not part of the deliverable)
.venv/

# derived figure plates - regenerated from assets/raw on every build
assets/processed/

# booklet page proofs (tools/build_booklet.py --pages) - proofing only
output/*-p[0-9][0-9].png

# scratch
/tmp/
*.log
