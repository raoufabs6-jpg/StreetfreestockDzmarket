# ============================================================
# AKMA Business — متغيرات البيئة (مثال)
# حاليًا لا يحتاج المشروع لأي متغيّر (التخزين في المتصفح).
# عند ربط قاعدة بيانات حقيقية لاحقًا، انسخ هذا الملف إلى
# .env.local محليًا وأضف القيم في Vercel → Settings → Env Vars
# ============================================================

# PostgreSQL / Supabase
# DATABASE_URL="postgresql://user:password@host:5432/akma"

# مثال لواجهة REST خارجية (إن وُجدت)
# NEXT_PUBLIC_API_URL="https://api.example.com"
